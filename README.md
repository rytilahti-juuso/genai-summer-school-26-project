# README

- first run data fetching script.
- then run temp.py to generate the interactive charts and the data files
- Move the data files into a correct folder¨
- run the 

# Other notes:

- The folder names are min-cluster size (so not clusters, but min cluster size), with min sample size set to none.

# TODO

- Make it so that the LLM ouput is strongly paired to the cluster label (currently it feeds it to the LLM and parses it from answer -> undeterministic, better approach would be to save it before, so that the cluster number label is always associated to the generated title).